version: '3.9'

services:
  wb-monitor:
    build: .
    container_name: wb-price-monitor
    env_file:
      - .env
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./db.sqlite3:/app/db.sqlite3
    restart: unless-stopped
